export interface IPost {
  id: string;
  title: string;
  body: string;
}

export interface IComment {
  id: string;
  name: string;
  email: string;
  body: string;
}

export interface IUser {
  id: string;
  username: string;
}
